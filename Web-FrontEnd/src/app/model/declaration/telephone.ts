export class Telephone {
  id: number;
  numeroPays: number;
  numeroTel: number;

  constructor(id: number, numeroPays: number, numeroTel: number) {
    this.id = id;
    this.numeroPays = numeroPays;
    this.numeroTel = numeroTel;
  }
}
